// TODO: introduce TimerParams interface

// TODO: define Timer class, reuse Evented

/*
 var timeoutId = setTimeout(function() {
 // this === window
 }, 2000);
 clearTimeout(timeoutId);

 var intervalId = setInterval(function() {
 // this === window
 }, 3000);
 clearInterval(intervalId);
 */
