/*
 random(max) function
 Returns psuedo random number between [0, and max)
 */
 //console.log("random:", random(100));

// use:
//Math.random(); // [0..1)

/*
 randomBetween(min, max) function
 Returns pseudo random number between [min, max)
 * */
// console.log("randomBetween:", random(13, 69));

/*
 * div(dividend, divisor)
 * Example 13 div 4 == 3
 * */
// console.log("div:", div(13, 4) === 3);
